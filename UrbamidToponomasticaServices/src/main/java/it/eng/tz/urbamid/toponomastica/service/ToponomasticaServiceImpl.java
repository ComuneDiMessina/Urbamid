package it.eng.tz.urbamid.toponomastica.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

/**
 * Service per la gestione dei grafici
 * @author Alessandro Paolillo
 */
@Service
public class ToponomasticaServiceImpl implements ToponomasticaService {

	private static final Logger logger = LoggerFactory.getLogger(ToponomasticaServiceImpl.class.getName());

	
}
